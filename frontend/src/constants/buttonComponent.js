import { ThemeColours } from './theme';

export const buttonTypes = () => {
    return {
        'main': {
            color: ThemeColours().dark,
            fontColor: ThemeColours().background,
        }
    }
}

