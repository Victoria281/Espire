import React from "react";
import styles from './styles.module.css'

const FlashcardManagement = ({  }) => {
    return (
        <div className={styles.flashcardManagementContainer} >
            FlashcardManagement
        </div>
    );
}

export default FlashcardManagement;
