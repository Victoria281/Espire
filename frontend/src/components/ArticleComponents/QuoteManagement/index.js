import React from "react";
import styles from './styles.module.css'

const QuoteManagement = ({  }) => {
    return (
        <div className={styles.quoteManagementContainer} >
            QuoteManagement
        </div>
    );
}

export default QuoteManagement;
